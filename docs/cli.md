# NeoFS CLI Command Overview

This document summarizes every command available under `docs/cli-commands` so you can quickly navigate the NeoFS CLI surface. Each entry includes its purpose, the most important flags, and a sample invocation. Refer to `neofs-cli --help` or `<command> --help` for the authoritative flag list, and open the linked files for the full autogenerated reference.

## 1. CLI Basics

- Root command: [`neofs-cli [flags]`](./cli-commands/neofs-cli.md)
- Common global flags: `-c/--config` (custom config file), `-v/--verbose`, `--version`, `-h/--help`
- Most subcommands accept contextual flags such as `-r/--rpc-endpoint`, `-w/--wallet`, `--address`, and `--session` to target specific networks, accounts, or sessions.

## 2. accounting: balances

| Command | Purpose | Key flags | Example |
| --- | --- | --- | --- |
| [`neofs-cli accounting balance`](./cli-commands/neofs-cli_accounting_balance.md) | Show the balance of a NeoFS account | `--address`, `--wallet` | `neofs-cli accounting balance --wallet wallet.json --address NSxxx` |

## 3. acl: access control

### 3.1 basic ACL

| Command | Purpose | Notes |
| --- | --- | --- |
| [`neofs-cli acl basic print`](./cli-commands/neofs-cli_acl_basic_print.md) | Display predefined basic ACL presets (`private`, `public-read`, etc.) | Useful when choosing `--basic-acl` during container creation |

### 3.2 extended ACL

| Command | Purpose | Key flags | Example |
| --- | --- | --- | --- |
| [`neofs-cli acl extended create`](./cli-commands/neofs-cli_acl_extended_create.md) | Build an eACL table from inline or file-based rules | `--cid`, `--rule/-r`, `--file/-f`, `--out/-o` | `neofs-cli acl extended create --cid <CID> -f rules.txt -o table.json` |
| [`neofs-cli acl extended print`](./cli-commands/neofs-cli_acl_extended_print.md) | Print an existing eACL table in readable form | `--table`, `--json` | `neofs-cli acl extended print --table table.json` |

## 4. bearer: bearer tokens

| Command | Purpose | Example |
| --- | --- | --- |
| [`neofs-cli bearer create`](./cli-commands/neofs-cli_bearer_create.md) | Create a bearer token from ACL rules | `neofs-cli bearer create --cid <CID> --file rules.txt --out bearer.json` |
| [`neofs-cli bearer print`](./cli-commands/neofs-cli_bearer_print.md) | Decode and display a bearer token | `neofs-cli bearer print --token bearer.json` |

## 5. completion: shell completion

- [`neofs-cli completion <shell>`](./cli-commands/neofs-cli_completion.md): generate completion scripts for `bash`, `zsh`, `fish`, etc.

## 6. container: container management

| Command | Focus | Key flags | Example |
| --- | --- | --- | --- |
| [`neofs-cli container create`](./cli-commands/neofs-cli_container_create.md) | Create and register a container | `--wallet`, `--policy/-p`, `--basic-acl`, `--attributes`, `--session` | `neofs-cli container create --wallet wallet.json --policy policy.json --basic-acl public-read` |
| [`neofs-cli container delete`](./cli-commands/neofs-cli_container_delete.md) | Remove a container | `--cid`, `--session`, `--force` | `neofs-cli container delete --cid <CID> --wallet wallet.json` |
| [`neofs-cli container get`](./cli-commands/neofs-cli_container_get.md) | Fetch container metadata | `--cid`, `--json`, `--to` | `neofs-cli container get --cid <CID> --json` |
| [`neofs-cli container list`](./cli-commands/neofs-cli_container_list.md) | List containers owned by an account | `--wallet` or `--address` | `neofs-cli container list --wallet wallet.json` |
| [`neofs-cli container list-objects`](./cli-commands/neofs-cli_container_list-objects.md) | Enumerate objects in a container | `--cid` plus optional filters | `neofs-cli container list-objects --cid <CID>` |
| [`neofs-cli container get-eacl`](./cli-commands/neofs-cli_container_get-eacl.md) | Download current eACL table | `--cid`, `--json`, `--to` | `neofs-cli container get-eacl --cid <CID> --json --to eacl.json` |
| [`neofs-cli container set-eacl`](./cli-commands/neofs-cli_container_set-eacl.md) | Upload a new eACL table | `--cid`, `--table`, `--session`, `--force` | `neofs-cli container set-eacl --cid <CID> --table eacl.json --wallet wallet.json` |
| [`neofs-cli container nodes`](./cli-commands/neofs-cli_container_nodes.md) | Show storage nodes serving the container | `--cid` | `neofs-cli container nodes --cid <CID>` |

## 7. control: storage node control

### 7.1 General commands

| Command | Purpose | Example |
| --- | --- | --- |
| [`neofs-cli control healthcheck`](./cli-commands/neofs-cli_control_healthcheck.md) | Probe storage node health | `neofs-cli control healthcheck --rpc-endpoint <host:port>` |
| [`neofs-cli control drop-objects`](./cli-commands/neofs-cli_control_drop-objects.md) | Force-delete objects | `neofs-cli control drop-objects --cid <CID> --oid <OID>` |
| [`neofs-cli control set-status`](./cli-commands/neofs-cli_control_set-status.md) | Change node service mode | `neofs-cli control set-status --status maintenance` |

### 7.2 Notary helpers

| Command | Purpose |
| --- | --- |
| [`neofs-cli control notary list`](./cli-commands/neofs-cli_control_notary_list.md) | List pending notary requests |
| [`neofs-cli control notary request`](./cli-commands/neofs-cli_control_notary_request.md) | Submit a notary request (e.g., container ops) |
| [`neofs-cli control notary sign`](./cli-commands/neofs-cli_control_notary_sign.md) | Sign and approve a notary request |

### 7.3 Object helpers

| Command | Purpose |
| --- | --- |
| [`neofs-cli control object list`](./cli-commands/neofs-cli_control_object_list.md) | List objects known to the node |
| [`neofs-cli control object status`](./cli-commands/neofs-cli_control_object_status.md) | Inspect object state |
| [`neofs-cli control object revive`](./cli-commands/neofs-cli_control_object_revive.md) | Restore soft-deleted objects |

### 7.4 Shard management

| Command | Purpose |
| --- | --- |
| [`neofs-cli control shards list`](./cli-commands/neofs-cli_control_shards_list.md) | Enumerate all shards |
| [`neofs-cli control shards set-mode`](./cli-commands/neofs-cli_control_shards_set-mode.md) | Switch shard modes (rw, ro, etc.) |
| [`neofs-cli control shards evacuate`](./cli-commands/neofs-cli_control_shards_evacuate.md) | Evacuate shard data |
| [`neofs-cli control shards restore`](./cli-commands/neofs-cli_control_shards_restore.md) | Re-import a shard |
| [`neofs-cli control shards dump`](./cli-commands/neofs-cli_control_shards_dump.md) | Dump shard metadata |
| [`neofs-cli control shards flush-cache`](./cli-commands/neofs-cli_control_shards_flush-cache.md) | Clear shard cache |

## 8. gendoc: auto-generated docs

- [`neofs-cli gendoc --dir <path>`](./cli-commands/neofs-cli_gendoc.md) scans the current command tree and emits Markdown (the files in this directory were produced this way).

## 9. netmap: network topology

| Command | Purpose | Example |
| --- | --- | --- |
| [`neofs-cli netmap epoch`](./cli-commands/neofs-cli_netmap_epoch.md) | Show current epoch | `neofs-cli netmap epoch --rpc-endpoint <host:port>` |
| [`neofs-cli netmap nodeinfo`](./cli-commands/neofs-cli_netmap_nodeinfo.md) | Describe a specific node | `neofs-cli netmap nodeinfo --node <pubkey>` |
| [`neofs-cli netmap netinfo`](./cli-commands/neofs-cli_netmap_netinfo.md) | Dump global network info | `neofs-cli netmap netinfo` |
| [`neofs-cli netmap snapshot`](./cli-commands/neofs-cli_netmap_snapshot.md) | Export a netmap snapshot | `neofs-cli netmap snapshot --out netmap.json` |

## 10. object: object service

| Command | Purpose | Typical use |
| --- | --- | --- |
| [`neofs-cli object put`](./cli-commands/neofs-cli_object_put.md) | Upload an object | Combine with `--attributes`, `--file`, `--cid` |
| [`neofs-cli object get`](./cli-commands/neofs-cli_object_get.md) | Download an object | `--cid`, `--oid`, `--file` |
| [`neofs-cli object head`](./cli-commands/neofs-cli_object_head.md) | Inspect object metadata | `--cid`, `--oid`, `--json` |
| [`neofs-cli object delete`](./cli-commands/neofs-cli_object_delete.md) | Delete an object | Optional `--tombstone` |
| [`neofs-cli object range`](./cli-commands/neofs-cli_object_range.md) | Download partial data by offset | `--offset`, `--length` |
| [`neofs-cli object hash`](./cli-commands/neofs-cli_object_hash.md) | Compute data hash | Integrity verification |
| [`neofs-cli object lock`](./cli-commands/neofs-cli_object_lock.md) | Lock an object against deletion | Provide lock duration |
| [`neofs-cli object search`](./cli-commands/neofs-cli_object_search.md) / [`searchv2`](./cli-commands/neofs-cli_object_searchv2.md) | Search objects via filters | `--cid`, `--filters`, `--limit` |
| [`neofs-cli object nodes`](./cli-commands/neofs-cli_object_nodes.md) | Show nodes storing the object | Useful for troubleshooting |

## 11. request: raw requests

| Command | Purpose |
| --- | --- |
| [`neofs-cli request create-container`](./cli-commands/neofs-cli_request_create-container.md) | Craft and send a raw container creation request (useful for debugging or notary flows) |

## 12. session: session tokens

| Command | Purpose | Example |
| --- | --- | --- |
| [`neofs-cli session create`](./cli-commands/neofs-cli_session_create.md) | Build PUT/DELETE/SETEACL session tokens | `neofs-cli session create --wallet wallet.json --verb put --cid <CID> --out put-session.json` |

## 13. util: helpers

| Subcommand | Purpose |
| --- | --- |
| [`neofs-cli util convert`](./cli-commands/neofs-cli_util_convert.md) | Convert object/container descriptors (JSON ↔ binary) |
| [`neofs-cli util convert eacl`](./cli-commands/neofs-cli_util_convert_eacl.md) | Convert eACL encodings |
| [`neofs-cli util keyer`](./cli-commands/neofs-cli_util_keyer.md) | Generate/inspect key pairs |
| [`neofs-cli util sign`](./cli-commands/neofs-cli_util_sign.md) | Generic signer |
| [`neofs-cli util sign bearer-token`](./cli-commands/neofs-cli_util_sign_bearer-token.md) | Sign bearer tokens |
| [`neofs-cli util sign session-token`](./cli-commands/neofs-cli_util_sign_session-token.md) | Sign session tokens |

## 14. Practical tips

- Most commands honor networking flags such as `--timeout` and `--ttl` to control request lifetime.
- Use `--await` on long-running operations (e.g., `container set-eacl --await`) to wait for FS chain confirmation.
- Compare CLI and SDK compatibility quickly via `neofs-cli --version`.

> This summary complements, but does not replace, the official `--help` output. Always consult the command-specific help for the complete flag set and constraints.

